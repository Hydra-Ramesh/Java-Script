/**
 * Casing actually use for Convert a String to Upper Case or Lower Case.
 */
let first="Ramesh"
let second=" Das"

let full_name=first.concat(second)
console.log(full_name)
console.log(full_name.toUpperCase())