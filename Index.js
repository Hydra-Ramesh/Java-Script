/**
 * It is actually a method of reverse indexing.
 * Here we have to print the key or index of a letter including in a string.
 */

let first="Ramesh"
let second=" Das"

let full_name=first.concat(second)
console.log(full_name)
console.log(full_name.indexOf('a'))